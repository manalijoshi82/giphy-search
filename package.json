{
  "name": "giphy-search",
  "version": "1.0.0",
  "description": "searching through giphy api",
  "main": "giphy-search.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Manali",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.7.2"
  }
}
